# 🛠️ REST API — Designer Portfolio

Документация по основным маршрутам API микросервисов проекта.

## 📑 Сервисы и их API

### User Service ([Swagger](http://localhost:8002/docs))

| Метод   | Эндпоинт         | Назначение                        | Доступ         |
|---------|------------------|-----------------------------------|----------------|
| POST    | /register        | Регистрация пользователя          | Публичный      |
| POST    | /login           | Вход, получение JWT               | Публичный      |
| GET     | /me              | Информация о себе (по JWT)        | Авторизованный |
| POST    | /logout          | Выход (логирование)               | Авторизованный |
| GET     | /logs            | Логи входов/выходов/ошибок        | Только admin   |

**Авторизация:**
- Для защищённых эндпоинтов используется JWT-токен (Bearer).

---

### Model Service ([Swagger](http://localhost:8000/docs))

| Метод   | Эндпоинт         | Назначение                        | Доступ         |
|---------|------------------|-----------------------------------|----------------|
| GET     | /models          | Получить список всех моделей      | Публичный      |
| GET     | /models/{id}     | Получить данные по одной модели   | Публичный      |
| POST    | /models          | Создать новую модель              | Только admin   |
| PATCH   | /models/{id}     | Обновить данные модели            | Только admin   |
| DELETE  | /models/{id}     | Удалить модель                    | Только admin   |

---

### Review Service ([Swagger](http://localhost:8001/docs))

| Метод   | Эндпоинт                 | Назначение                        | Доступ         |
|---------|--------------------------|-----------------------------------|----------------|
| POST    | /models/{id}/reviews     | Добавить отзыв и оценку           | Только user    |
| GET     | /reviews                 | Получить список всех отзывов      | Только admin   |
| PATCH   | /reviews/{id}            | Редактировать свой отзыв          | Только user    |
| DELETE  | /reviews/{id}            | Удалить свой или любой отзыв      | user/admin     |

---

## 🔐 Авторизация

- Для большинства защищённых эндпоинтов используется JWT (Bearer).
- Для некоторых административных операций может использоваться дополнительная проверка роли.

## 📝 OpenAPI спецификации

- [User Service OpenAPI](../../user-service/README.md)
- [Model Service OpenAPI](../../model-service/README.md)
- [Review Service OpenAPI](../../review-service/README.md)
